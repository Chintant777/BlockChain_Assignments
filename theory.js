/**
 * 1: generators can return multiple values using yield keyword
 *    function can return only one value
 *    
 *    generators are memory efficient as it generates value at one time
 *    function do some process and store some values to execute for returing value.
 * 
 */

/**
 * 2: function* demoGenerator(){
 *      
 *      yield 1;
 *      yield 2;
 * }
 */

/**
 * 3: generators can be used for iteration as it returns iteration object.
 * untill some condition we can call next() method and work. whenever it counters
 * yield it suspends transaction for while untill next() called.
 */